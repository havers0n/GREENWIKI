import React from 'react';
import { Header } from '../../../widgets';

/**
 * DebugHeader - абстракция для Header в отладочных инструментах
 * Инкапсулирует использование Header виджета для отладочных страниц
 */
const DebugHeader: React.FC = () => {
  return <Header />;
};

export default DebugHeader;
