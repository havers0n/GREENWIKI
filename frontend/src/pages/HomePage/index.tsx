
import React from 'react';
import BlockRenderer from 'widgets/BlockRenderer';

const HomePage = (): React.ReactNode => {
    return <BlockRenderer pageIdentifier="homepage" />;
};

export default HomePage;
