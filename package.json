{
  "name": "my-forum",
  "version": "1.0.0",
  "description": "Forum application with backend, frontend and shared types",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm --recursive build",
    "test": "pnpm --recursive test",
    "lint": "pnpm --recursive lint",
    "gen:types": "supabase gen types typescript --linked > packages/db-types/src/index.ts"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^16.3.0",
    "@types/react": "19.1.7",
    "@types/react-dom": "19.1.7",
    "jsdom": "^26.1.0",
    "supabase": "^2.39.2",
    "typescript": "5.8.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
